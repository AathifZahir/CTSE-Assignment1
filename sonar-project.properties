# SonarCloud: organization and projectKey are set in GitHub Actions (-D args).
# Add GitHub secrets: SONAR_ORGANIZATION, SONAR_PROJECT_KEY (and existing SONAR_TOKEN).

sonar.sources=user-service/src,booking-service/src,event-service/src,payment-service/src
sonar.exclusions=**/node_modules/**,**/coverage/**,**/dist/**,**/.env,**/*.min.js
